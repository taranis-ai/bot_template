# Ask the same questions you had before
project_name:
  type: str
  help: Project Name

package_name:
  default: "{{ project_name.lower().replace(' ', '_').replace('-', '_') }}"
  when: false
  type: str
